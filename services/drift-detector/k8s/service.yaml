apiVersion: v1
kind: Service
metadata:
  name: drift-detector
  labels:
    app: drift-detector
spec:
  type: ClusterIP
  selector:
    app: drift-detector
  ports:
    - name: http
      port: 80
      targetPort: http
