apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: rollbackops

resources:
  - model-server
  - drift-detector
  - rollback-controller
  - monitoring
  - ingress.yaml
